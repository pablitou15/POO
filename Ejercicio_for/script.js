function numeroMayor(){
  let arrayNum = [];
  let numMayor = 0;

  for(let i = 0; i < 5; i++){
      let num = parseInt(prompt("Ingrese un numero para determinar Mayor"));
    arrayNum.push(num);}
    if(numMayor > num){
        numMayor = num;
    }
}